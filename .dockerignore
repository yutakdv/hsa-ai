# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/

# 가상환경 (컨테이너 내부에서 재설치)
.venv/

# 테스트 / 린트 캐시
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# 시크릿 (env_file로 주입)
.env
.env.*

# LlamaIndex 인덱스: 이미지에 baking (ECS는 볼륨 없음 → 기동 시 load_index_from_storage로 로드만,
# 재임베딩 비용/지연 제거). dev는 docker-compose 볼륨 마운트가 런타임에 덮어씀.
# 정책 문서 변경/임베딩 모델·chunk 변경 시 인덱스 재생성→git commit 필요.

# OS / IDE
.DS_Store
.idea/
.vscode/

# Git
.git/
.github/

# 빌드 결과물
build/
dist/

# 개발/문서 전용 디렉터리 (프로덕션 이미지 불필요)
tests/
evals/
docs/
harness/
